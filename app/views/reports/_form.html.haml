= simple_form_for(@report) do |f|
  = f.error_notification

  .form-inputs
    = f.association :report_type
    = f.association :report_type_options
    = f.input :name
    = f.input :notes
    = f.input :start_date
    = f.input :end_date
    = f.input :logo_enabled, label: 'Logo Enabled?'

  .form-actions
    = f.button :submit
